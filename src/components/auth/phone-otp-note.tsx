/**
 * The mock-mode hint shown under the phone field while no SMS provider is
 * configured. Separate file so the form component stays purely behavioural.
 */
export const integrationsNote =
  "SMS is in test mode — the code prints to the server console instead of texting.";
